pipeline {
    agent any
    stages { 
        stage('unit-test') {
            steps {
                echo 'unit-test'
            }
        }
		
		stage('integration-test') {
            steps {
                echo 'integration-test'
            }
        }
    }
}